<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? "#{yield(:title)} — Kiqr" : 'Kiqr' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', 'https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;700&display=swap' %>
    <%= javascript_importmap_tags %>
    
    <style type="text/css">
    body { font-family: 'Ubuntu', sans-serif; }
    </style>
    <%= yield :head %>
  </head>
  <body class="grid grid-cols-12 min-h-screen">
    <div class="col-span-12 lg:col-span-7 bg-gray-50">
      <div class="flex justify-center items-center min-h-full">
        <div class="container">
          <div class="max-w-md mx-auto">
            <div class="text-center">
              <%= yield(:form_headers) %>
            </div>
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
    <div class="hidden lg:block lg:col-span-5 bg-gradient-to-tl from-gray-900 to-purple-900">
      <div class="flex flex-col justify-center space-y-5 min-h-full items-center xl:items-start xl:pl-20">
        <div class="space-y-2">
          <h1 class="text-3xl font-bold text-white">Welcome to</h1>
          <h1 class="text-2xl font-light text-gray-100">your Kiqr starter template</h1>
        </div>
        <p class="text-white text-xs">Edit this layout at <code class="bg-gray-700 p-1 ml-1">app/views/auth.html.erb</code></p>

        <div class="space-x-2 pt-10">
          <%= link_to 'View source', 'https://github.com/kiqr/kiqr', class: 'bg-white text-purple-500 px-4 py-2 text-xs rounded-md hover:underline', target: '_blank' %>
          <%= link_to 'Read the documentation', 'https://docs.kiqr.dev', class: 'bg-white text-purple-500 px-4 py-2 text-xs rounded-md hover:underline', target: '_blank' %>
        </div>
      </div>
    </div>
  </body>
</html>