<% content_for :title, 'Forgot your password?' %>

<% content_for :form_headers do %>
  <h1 class="my-3 text-3xl font-semibold text-gray-700 dark:text-gray-200">Forgot your password?</h1>
  <p class="text-gray-500 dark:text-gray-400">No worries, we'll help you recover it</p>
<% end %>

<%= render 'shared/flash_messages' %>

<%= render 'form' %>

<%= render "devise/shared/links" %>
